{
  "name": "context-engineering",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "updator": "node --enable-source-maps dist/updator.js",
    "build": "tsc -p tsconfig.json",
    "db:init": "node scripts/init-db.cjs",
    "db:seed": "node scripts/seed-instructions.cjs",
    "apply": "npm run build && npm run updator"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.9",
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3"
  }
}

